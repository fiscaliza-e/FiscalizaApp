<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/fiscalizaapp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fiscaliza">
        <activity
            android:name=".composables.card.CategoryCard"
            android:exported="false"
            android:label="@string/title_activity_category_card"
            android:theme="@style/Theme.Fiscaliza" />
        <activity
            android:name=".composables.card.CardComposable"
            android:exported="false"
            android:label="@string/title_activity_card_composable"
            android:theme="@style/Theme.Fiscaliza" />
        <activity
            android:name=".composables.card.CardBaseComposable"
            android:exported="false"
            android:label="@string/title_activity_card_base_composable"
            android:theme="@style/Theme.Fiscaliza" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Fiscaliza">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>